spring:
  application:
    name: "Users"
  datasource:
    url: jdbc:postgresql://localhost:5433/portfolio_chatbot
    username: postgres
    password: 'postgres'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
#  servlet:
#    multipart:
#      enabled: true

mistral:
  api:
    key: ${MISTRAL_API_KEY}  # From environment variables
    base-url: https://api.mistral.ai/v1
